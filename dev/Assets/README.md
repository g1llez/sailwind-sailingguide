# dev/Assets/

Tracked authoring files (not loaded by the game directly).

```
Assets/
  shared/                 Icons (`icons/final/`), FONTS.md pointer
  guides/
    archipelago/
      pages/
        <id>/             e.g. alankh
          01-cover.svg    Inkscape master + matching `.txt` source
          02-legend.svg
          …
```

Export 2048×2048 PNGs to `pages/en/<id>/` and `pages/fr/<id>/` (same file names per language).

Notes, fonts, WIP: `../_workspace/`.
