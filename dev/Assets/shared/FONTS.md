# Fonts (local only)

EB Garamond files used for parchemin layout live in:

`dev/_workspace/global/fonts/EBGaramond/` (local only)

Download again from [Google Fonts — EB Garamond](https://fonts.google.com/specimen/EB+Garamond) (OFL license). See `OFL.txt` in that folder.

Not included in the Git repository (binaries + personal notes stay under `_workspace/`).
