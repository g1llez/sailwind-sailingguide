# dev/

Authoring and reference exports — not loaded by the game at runtime.

| Folder | Role |
|--------|------|
| **Assets/** | `guides/<id>/svg/`, `shared/icons/final/` (notes & fonts → `_workspace/`) |
| **_workspace/** | Heavy WIP (GIMP, Pureref, SVG iterations) — local only, gitignored |
| **reference/** | How to extract vanilla assets locally — [README](reference/README.md) |
| **plugin/** | BepInEx mod — [README](plugin/README.md), `build.ps1` |

Vanilla PNG/maps live in `_workspace/reference/ingame/<version>/` (gitignored). See [reference/README.md](reference/README.md).

See [_workspace/README.md](_workspace/README.md) for what lives outside Git.
