# dev/

Authoring and reference exports — not loaded by the game at runtime.

| Folder | Role |
|--------|------|
| **Assets/** | `guides/<id>/svg/`, `shared/icons/final/` (notes & fonts → `_workspace/`) |
| **_workspace/** | Heavy WIP (GIMP, Pureref, SVG iterations, vanilla refs) — local only, gitignored |
| **plugin/** | BepInEx mod — [README](plugin/README.md), `build.ps1` |

Vanilla PNG/maps: export with SailwindHack → `_workspace/reference/ingame/<version>/` (gitignored).

See [_workspace/README.md](_workspace/README.md) for what lives outside Git.
