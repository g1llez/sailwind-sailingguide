# French pages

Export Inkscape/GIMP parchemins here with the **same file names** as `pages/en/alankh/` (e.g. `01-cover.png`).

Install or configure:

```ini
PagesDirectory = pages/fr
```

The FR release zip ships this folder instead of `pages/en/`.
